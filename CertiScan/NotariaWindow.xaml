<Window x:Class="CertiScan.NotariaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sw="clr-namespace:System.Windows;assembly=PresentationCore"
        Title="CertiScan - Configuración de Notaría" 
        Height="620" Width="580" 
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        FontFamily="Segoe UI"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBlack" Color="#213449"/>
        <SolidColorBrush x:Key="AccentLightGray" Color="#F0F0F0"/>
        <SolidColorBrush x:Key="TextLight" Color="White"/>
        <SolidColorBrush x:Key="ErrorRed" Color="#C0392B"/>
        <SolidColorBrush x:Key="BorderHighlight" Color="#CCCCCC"/>

        <Style x:Key="InputStyleNotaria" TargetType="{x:Type TextBox}">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderHighlight}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBlack}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BotonEstiloMainWindow" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{StaticResource PrimaryBlack}"/>
            <Setter Property="Foreground" Value="{StaticResource TextLight}"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BotonCancelarEstilo" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource ErrorRed}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#922B21"/>
                    <Setter Property="TextBlock.TextDecorations" Value="{x:Static sw:TextDecorations.Underline}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="{StaticResource AccentLightGray}" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="270" ShadowDepth="2" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource PrimaryBlack}" Padding="20" CornerRadius="15,15,0,0">
                <Grid>
                    <TextBlock Text="Datos de la Notaría" Foreground="White" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Content="✕" HorizontalAlignment="Right" Foreground="White" Background="Transparent" BorderThickness="0" FontSize="18" Click="Cancelar_Click"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="30,20,30,20">
                    <TextBlock Text="Nombre del Notario" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBlack}" Margin="2,0,0,5"/>
                    <TextBox x:Name="txtNombre" Style="{StaticResource InputStyleNotaria}" Margin="0,0,0,15"/>

                    <TextBlock Text="Número de Notaría" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBlack}" Margin="2,0,0,5"/>
                    <TextBox x:Name="txtNumero" Style="{StaticResource InputStyleNotaria}" Margin="0,0,0,15"/>

                    <TextBlock Text="Dirección Completa" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBlack}" Margin="2,0,0,5"/>
                    <TextBox x:Name="txtDireccion" Style="{StaticResource InputStyleNotaria}" Margin="0,0,0,15"/>

                    <TextBlock Text="Teléfono (10 dígitos)" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBlack}" Margin="2,0,0,5"/>
                    <TextBox x:Name="txtTelefono" Style="{StaticResource InputStyleNotaria}" Margin="0,0,0,15" MaxLength="10"/>

                    <TextBlock Text="Email de Contacto" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBlack}" Margin="2,0,0,5"/>
                    <TextBox x:Name="txtEmail" Style="{StaticResource InputStyleNotaria}" Margin="0,0,0,25"/>

                    <Button Content="GUARDAR Y SINCRONIZAR" 
                            Style="{StaticResource BotonEstiloMainWindow}" 
                            Click="Guardar_Click" 
                            Height="45"/>

                    <Button Content="Cancelar" 
                            Style="{StaticResource BotonCancelarEstilo}" 
                            Click="Cancelar_Click" 
                            Margin="0,15,0,0" 
                            Height="30"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>